/**
 * @deprecated Import from `@/services/platforms/publish` instead.
 * Kept for backward compatibility with existing imports.
 */
export {
  publishToPlatform,
  type PublishPayload,
  type PublishResult,
} from "@/services/platforms/publish";

export { getPlatformConnector, listConnectorKeys } from "@/services/platforms/factory";
